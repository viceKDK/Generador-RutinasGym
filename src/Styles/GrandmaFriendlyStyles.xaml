<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colores de alto contraste amigables para abuela -->
    <SolidColorBrush x:Key="GrandmaBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="GrandmaTextBrush" Color="#000000"/>
    <SolidColorBrush x:Key="GrandmaPrimaryBrush" Color="#0078D4"/>
    <SolidColorBrush x:Key="GrandmaSecondaryBrush" Color="#106EBE"/>
    <SolidColorBrush x:Key="GrandmaSuccessBrush" Color="#107C10"/>
    <SolidColorBrush x:Key="GrandmaWarningBrush" Color="#FF8C00"/>
    <SolidColorBrush x:Key="GrandmaErrorBrush" Color="#D13438"/>
    <SolidColorBrush x:Key="GrandmaLightGrayBrush" Color="#F3F2F1"/>
    <SolidColorBrush x:Key="GrandmaBorderBrush" Color="#CCCCCC"/>

    <!-- Estilo para botones extra-grandes -->
    <Style x:Key="GrandmaButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GrandmaPrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="MinWidth" Value="200"/>
        <Setter Property="Padding" Value="20,15"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource GrandmaSecondaryBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="ContentPresenter"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        ContentTransitions="{TemplateBinding ContentTransitions}"
                                        Foreground="{TemplateBinding Foreground}"/>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{StaticResource GrandmaSecondaryBrush}"/>
                                        <Setter Target="RootBorder.BorderThickness" Value="3"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#0B5394"/>
                                        <Setter Target="RootBorder.BorderThickness" Value="4"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#CCCCCC"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="#666666"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Estilo para botón de exportar (verde) -->
    <Style x:Key="GrandmaExportButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GrandmaSuccessBrush}"/>
        <Setter Property="BorderBrush" Value="#0F5132"/>
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="FontSize" Value="24"/>
    </Style>

    <!-- Estilo para botón secundario -->
    <Style x:Key="GrandmaSecondaryButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GrandmaLightGrayBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GrandmaBorderBrush}"/>
        <Setter Property="MinWidth" Value="180"/>
    </Style>

    <!-- Estilo para TextBox grandes -->
    <Style x:Key="GrandmaTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="MinHeight" Value="50"/>
        <Setter Property="Padding" Value="15,12"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource GrandmaBorderBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
        <Setter Property="CornerRadius" Value="6"/>
    </Style>

    <!-- Estilo para ComboBox grandes -->
    <Style x:Key="GrandmaComboBoxStyle" TargetType="ComboBox">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="MinHeight" Value="50"/>
        <Setter Property="Padding" Value="15,12"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource GrandmaBorderBrush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
        <Setter Property="CornerRadius" Value="6"/>
    </Style>

    <!-- Estilo para CheckBox grandes -->
    <Style x:Key="GrandmaCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
    </Style>

    <!-- Estilo para etiquetas grandes -->
    <Style x:Key="GrandmaLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="5,10,5,5"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Estilo para títulos grandes -->
    <Style x:Key="GrandmaTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="10,15"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaPrimaryBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Estilo para subtítulos -->
    <Style x:Key="GrandmaSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="10,10,10,15"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaSecondaryBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Estilo para mensajes de estado -->
    <Style x:Key="GrandmaStatusStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <!-- Estilo para ProgressBar grande -->
    <Style x:Key="GrandmaProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Background" Value="{StaticResource GrandmaLightGrayBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaPrimaryBrush}"/>
    </Style>

    <!-- Estilo para paneles con bordes claros -->
    <Style x:Key="GrandmaPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource GrandmaBorderBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <!-- Estilo para contenedor principal -->
    <Style x:Key="GrandmaMainContainerStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource GrandmaBackgroundBrush}"/>
        <Setter Property="Margin" Value="20"/>
    </Style>

    <!-- ========== RESPONSIVE DESIGN STYLES ========== -->

    <!-- Adaptive Grid for different screen sizes -->
    <Style x:Key="ResponsiveGridStyle" TargetType="Grid">
        <Setter Property="Margin" Value="12"/>
        <Setter Property="Background" Value="{StaticResource GrandmaBackgroundBrush}"/>
    </Style>

    <!-- Responsive button that adapts to screen size -->
    <Style x:Key="ResponsiveButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="MinWidth" Value="120"/>
        <Setter Property="MaxWidth" Value="280"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="8"/>
    </Style>

    <!-- Adaptive text that scales with screen size -->
    <Style x:Key="AdaptiveTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Foreground" Value="{StaticResource GrandmaTextBrush}"/>
    </Style>

    <!-- ========== TRANSITION ANIMATIONS ========== -->

    <!-- Enhanced button style with smooth transitions -->
    <Style x:Key="AnimatedButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <PointerUpThemeTransition/>
                    <PointerDownThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Animated panel with entrance effect -->
    <Style x:Key="AnimatedPanelStyle" BasedOn="{StaticResource GrandmaPanelStyle}" TargetType="Border">
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <EntranceThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== ACCESSIBILITY IMPROVEMENTS ========== -->

    <!-- High contrast style for better accessibility -->
    <Style x:Key="HighContrastButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="BorderThickness" Value="3"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="MinHeight" Value="70"/>
    </Style>

    <!-- Focus-friendly style for keyboard navigation -->
    <Style x:Key="KeyboardFriendlyButtonStyle" BasedOn="{StaticResource GrandmaButtonStyle}" TargetType="Button">
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="UseSystemFocusVisuals" Value="True"/>
    </Style>

</ResourceDictionary>